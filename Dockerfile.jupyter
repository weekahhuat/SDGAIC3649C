FROM jupyter/pyspark-notebook:latest

# Install data science and database libraries
RUN pip install --upgrade pip && \
    pip install \
        pandas numpy scikit-learn seaborn matplotlib \
        mysql-connector-python pymongo hdfs
